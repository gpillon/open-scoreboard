import { PositionToColorPipe } from './position-to-color.pipe';

describe('PositionToColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PositionToColorPipe();
    expect(pipe).toBeTruthy();
  });
});
